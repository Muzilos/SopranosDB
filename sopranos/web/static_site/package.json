{
  "name": "sopranosdb-static-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Front-end for SopranosDB: thin client over the D1-backed query Worker (FTS5 search, facets, live view counts).",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
